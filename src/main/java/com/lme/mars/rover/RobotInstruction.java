package com.lme.mars.rover;

/**
 * Instruction codes for the Robot. New instruction codes should be added
 * here with the corresponding Command added to the Robot.
 */
public enum RobotInstruction {
    R,
    L,
    F;
}
